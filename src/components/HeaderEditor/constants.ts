import { EMPTY_STRING } from '../../constants/constants';
import { INewHeaderFormData } from './HeaderEditor';

export const EMPTY_NEW_HEADER_FORM_DATA: INewHeaderFormData = {
  headerName: EMPTY_STRING,
  headerValue: EMPTY_STRING,
};
