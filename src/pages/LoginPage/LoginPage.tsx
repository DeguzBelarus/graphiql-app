import { FC } from 'react';

import { Header } from '../../components/Header/Header';
import './LoginPage.scss';

export const LoginPage: FC = () => {
  return (
    <>
      <Header />
      <div className="login-page-wrapper">LoginPage works!</div>
    </>
  );
};
